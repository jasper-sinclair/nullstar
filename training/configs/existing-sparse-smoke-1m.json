{
  "batch_size": 256,
  "epochs": 1,
  "learning_rate": 0.001,
  "lr_decay_factor": 0.5,
  "lr_patience": 3,

  "num_workers": 0,
  "device": "cpu",

  "scale": 128,
  "l1_size": 256,

  "training_file": "training_sparse_stm.bin",
  "offset_index_path": "smoke\\training_sparse_stm_prefix_1m.offsets.u64",
  "dataset_sample_limit": 1000000,
  "validation_split": 0.05,
  "seed": 42,
  "sampler_block_size": 10000,

  "checkpoint_path": "smoke\\checkpoint_stm_smoke_1m.pt",
  "mid_checkpoint_path": "smoke\\checkpoint_mid_epoch_stm_smoke_1m.pt",
  "best_model_path": "smoke\\best_model_stm_smoke_1m.pt",
  "export_path": "smoke\\network_stm_smoke_1m.bin",
  "log_path": "smoke\\training_stm_smoke_1m.log",
  "pipeline_log_path": "smoke\\training_stm_smoke_1m_pipeline.log",
  "save_epoch_networks": false,

  "use_amp": false,
  "label_smoothing": 0.0,
  "grad_clip": 1.0,
  "feature_dropout": 0.0,

  "verification_epd": "..\\DATA\\MASTER\\training_stm.txt",
  "verify_export_parity": true,
  "export_parity_samples": 128,
  "export_parity_max_logit_error": 0.25,

  "log_every": 500,
  "mid_checkpoint_every": 0
}
